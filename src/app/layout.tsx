import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Keerthirajan Murugesan | Graduate AI Engineer",
  description: "Portfolio of Keerthirajan Murugesan, a Graduate AI Engineer specializing in NLP, ML, and MLOps.",
  keywords: ["AI Engineer", "Data Science", "NLP", "Machine Learning", "Portfolio", "Keerthirajan Murugesan"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="scroll-smooth">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-background text-foreground`}
      >
        {children}
      </body>
    </html>
  );
}
